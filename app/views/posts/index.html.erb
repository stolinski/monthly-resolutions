<h1><%= @month %></h1>


<% @posts.each do |post| %>
<div class="res">
    <h3 class="res-title"><%= post.title %></h3>
    <span><%= link_to(edit_post_path(post)) do %><i class="icon-edit"></i>
    <% end %>
    <%= link_to(post, :method => :delete, :data => { :confirm => 'Are you sure?' }) do %><i class="icon-remove-sign"></i>
        <% end %>
    </span>
</div>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
